name: LLMOps PR Evaluator

on:
  pull_request:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read
  pull-requests: write # IZIN PENTING: Supaya bot boleh komen di PR

jobs:
  compare-performance:
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      # --- STEP 1: TEST BASELINE (MAIN BRANCH) ---
      - name: Checkout Main Branch
        uses: actions/checkout@v4
        with:
          ref: main          # Ambil kodingan versi Main (Stabil)
          path: main-code    # Simpan di folder khusus

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: pip install boto3

      - name: Run Evaluation on MAIN (Baseline)
        env:
          AGENT_ID: ${{ secrets.AGENT_ID }}
          AWS_REGION: us-east-1
        run: |
          cd main-code
          python scripts/evaluate.py
          mv metrics.json ../baseline_metrics.json
          echo "✅ Baseline evaluation done."

      # --- STEP 2: TEST CURRENT BRANCH (PR) ---
      - name: Checkout PR Branch
        uses: actions/checkout@v4
        with:
          path: pr-code      # Ambil kodingan versi PR (Eksperimen)

      - name: Run Evaluation on PR (New Code)
        env:
          AGENT_ID: ${{ secrets.AGENT_ID }}
          AWS_REGION: us-east-1
        run: |
          cd pr-code
          python scripts/evaluate.py
          mv metrics.json ../current_metrics.json
          echo "✅ Current PR evaluation done."

      # --- STEP 3: COMPARE & COMMENT ---
      - name: Post PR Comment Report
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            try {
                const baseline = JSON.parse(fs.readFileSync('baseline_metrics.json', 'utf8'));
                const current = JSON.parse(fs.readFileSync('current_metrics.json', 'utf8'));

                // --- HELPERS ---
                const clean = (text) => {
                    if (!text) return "-";
                    return text.replace(/\|/g, "I").replace(/[\r\n]+/g, " ").trim();
                };
                
                const cleanTable = (text) => {
                    if (!text) return "-";
                    return text.replace(/\|/g, "I").replace(/[\r\n]+/g, "<br>").trim();
                };

                // --- 1. SCORECARD ---
                const diff = current.accuracy - baseline.accuracy;
                let icon = '➖';
                if (diff > 0) icon = '🚀 (Improved)';
                if (diff < 0) icon = '⚠️ (Degraded)';

                // --- 2. VALIDATION REPORT (COMPARING MAIN VS PR) ---
                const baseResults = baseline.results;
                const currResults = current.results;
                
                // Header Tabel (Sekarang 5 Kolom)
                let validationRows = "| Status | Input Snippet | Ground Truth | Main Branch | This PR |\n| :---: | :--- | :--- | :--- | :--- |\n";
                
                // Pakai For Loop biasa biar bisa ambil index 'i'
                for (let i = 0; i < currResults.length; i++) {
                    const item = currResults[i];     // Hasil PR (Current)
                    const baseItem = baseResults[i]; // Hasil Main (Baseline)

                    // Logic Icon
                    const statusIcon = item.is_correct ? "✅" : "❌";
                    
                    // Format Data
                    const inputShort = clean(item.input).substring(0, 30) + "...";
                    const expected = clean(item.expected);
                    
                    // Ambil Label Prediksi
                    const mainPred = clean(baseItem.actual_category || "N/A");
                    const prPred = clean(item.actual_category || "N/A");
                    
                    // Formatting: Kalau Salah di-Bold
                    const prDisplay = item.is_correct ? prPred : `**${prPred}**`;
                    
                    // Rakit Baris
                    validationRows += `| ${statusIcon} | ${inputShort} | ${expected} | ${mainPred} | ${prDisplay} |\n`;
                }

                // --- 3. INFERENCE REPORT (INDONESIAN ANALYSIS) ---
                let inferenceRows = "";
                if (current.demo_results && current.demo_results.length > 0) {
                    inferenceRows = current.demo_results.map(item => {
                        const colInput = cleanTable(item.input);
                        const colLabel = `**${cleanTable(item.category)}**`;
                        const colReason = cleanTable(item.reason); // Ini nanti isinya Bhs Indo
                        
                        let colReply = cleanTable(item.reply);
                        if (colReply === "NO_REPLY") colReply = "<em>(No Reply)</em>";

                        return `| ${colInput} | ${colLabel} | ${colReason} | ${colReply} |`;
                    }).join("\n");
                } else {
                    inferenceRows = "| - | - | - | - |";
                }

                // --- 4. RAKIT LAPORAN ---
                let body = `
                ### 🤖 LLMOps Report: ${context.sha.substring(0, 7)}
                
                #### 📊 Scorecard
                | Metric | Main Branch | This PR | Change |
                | :--- | :---: | :---: | :---: |
                | **Accuracy** | ${baseline.accuracy.toFixed(2)}% | ${current.accuracy.toFixed(2)}% | ${icon} |
                
                ---
                
                #### 📋 Validation Results (Side-by-Side)
                _Membandingkan prediksi **Main Branch** vs **This PR**. Status merah (❌) berarti PR ini salah prediksi._
                
                ${validationRows}
                
                ---
                
                #### 🧠 AI Analysis & Reasoning (Indonesian)
                _Analisis AI terhadap data baru (Live Inference)._
                
                | Input | Label | Analisis & Alasan | Rekomendasi Balasan |
                | :--- | :--- | :--- | :--- |
                ${inferenceRows}
                `;

                // Anti-Indentasi (Wajib ada biar tabel ga pecah)
                body = body.replace(/^ +/gm, '');

                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: body
                });

            } catch (error) {
                console.log("Error generating report:", error);
                core.setFailed("Failed to generate report.");
            }
